<% if user_signed_in? && current_user.bookmark?(restaurant) %>
  <%= render 'shared/unbookmark', { restaurant: restaurant } %>
<% else %>
  <%= render 'shared/bookmark', { restaurant: restaurant } %>
<% end %>
